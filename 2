#include <iostream>
using namespace std;
int main()
{
    int N, i, a, b=0, c, d, n, j;
    cin >> N;
    n = N;
    int arr[N];
    int barr[N];
    int carr[N];
    for (i = 0; i < N; i++) {
        cin >> arr[i];
    }
    barr[0] = arr[0];
    for (i = 0; i < N; i++) {
        a = arr[i];
        c = 0;
        for (j = 0; j < N; j++) {
            if (arr[j] == a) c++;
        }
        if (c == 2) {
            carr[i] = arr[i];
        }
    }
    for (i = 0; i < N; i++) {
        d = 1;
        for (j = 0; j < N; j++) {
            if (arr[i] == carr[j]) d = 0;
        }
        if (d == 1) {
            barr[b] = arr[i];
            b++;
        }
    }
    for (i = 0; i < b; i++) {
        cout << barr[i];
    }
 
 
}
