#include <iostream>
using namespace std;
int main()
{
    int N, i, a, b=0, c, d, n, j;
    cin >> N;
    n = N;
    int A[N];
    int B[N];
    int C[N];
    for (i = 0; i < N; i++) {
        cin >> A[i];
    }
    B[0] = A[0];
    for (i = 0; i < N; i++) {
        a = A[i];
        c = 0;
        for (j = 0; j < N; j++) {
            if (A[j] == a) c++;
        }
        if (c == 2) {
            C[i] = A[i];
        }
    }
    for (i = 0; i < N; i++) {
        d = 1;
        for (j = 0; j < N; j++) {
            if (A[i] == C[j]) d = 0;
        }
        if (d == 1) {
            B[b] = A[i];
            b++;
        }
    }
    for (i = 0; i < b; i++) {
        cout << B[i];
    }
 
 
}
